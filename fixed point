#include <stdio.h>
#include <math.h>

#define TOL 0.000001
#define MAX_ITER 100


double g(double x)
{
    return (x*x*x +  x*x - 3*x - 3);   
}

int main()
{
    double x0, x1;
    double erroe+ 0.0001;
    int iteration = 0;

    printf("Enter initial guess: ");
    scanf("%lf", &x0);

    do
    {
        if (df(x0) == 0)
        {
        	printf("Derivative is zero, Method fails\n");
        	return 0;
		}
		x1

    printf("\nSolution not obtained within maximum iterations\n");
    return 1;
}
